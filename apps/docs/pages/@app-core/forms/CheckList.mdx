
import { CheckList, getDocumentationProps } from '@app/core/forms/CheckList.styled'
import { ComponentDocs } from '@app/core/mdx/ComponentDocs'
import { FileTree } from 'nextra/components'

# CheckList

```tsx
import { CheckList } from '@app/components/CheckList'
```

<ComponentDocs
    component={CheckList}
    docsConfig={getDocumentationProps}
/>

### Location

You can find the source of the `CheckList` component in the following location:

<FileTree>
    <FileTree.Folder name="features" defaultOpen>
        <FileTree.Folder name="@app-core" defaultOpen>
            <FileTree.Folder name="forms" defaultOpen>
                <FileTree.File name="CheckList.styled.tsx" />
            </FileTree.Folder>
        </FileTree.Folder>
    </FileTree.Folder>
</FileTree>
