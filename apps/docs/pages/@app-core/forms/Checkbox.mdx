
import { Checkbox, getDocumentationProps } from '@app/core/forms/Checkbox.styled'
import { ComponentDocs } from '@app/core/mdx/ComponentDocs'
import { FileTree } from 'nextra/components'

# Checkbox

```tsx
import { Checkbox } from '@app/components/Checkbox'
```

<ComponentDocs
    component={Checkbox}
    docsConfig={getDocumentationProps}
/>

### Location

You can find the source of the `Checkbox` component in the following location:

<FileTree>
    <FileTree.Folder name="features" defaultOpen>
        <FileTree.Folder name="@app-core" defaultOpen>
            <FileTree.Folder name="forms" defaultOpen>
                <FileTree.File name="Checkbox.styled.tsx" />
            </FileTree.Folder>
        </FileTree.Folder>
    </FileTree.Folder>
</FileTree>
