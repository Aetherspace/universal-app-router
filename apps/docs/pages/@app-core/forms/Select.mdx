
import { Select, getDocumentationProps } from '@app/core/forms/Select.styled'
import { ComponentDocs } from '@app/core/mdx/ComponentDocs'
import { FileTree } from 'nextra/components'

# Select

```tsx
import { Select } from '@app/components/Select'
```

<ComponentDocs
    component={Select}
    docsConfig={getDocumentationProps}
/>

### Location

You can find the source of the `Select` component in the following location:

<FileTree>
    <FileTree.Folder name="features" defaultOpen>
        <FileTree.Folder name="@app-core" defaultOpen>
            <FileTree.Folder name="forms" defaultOpen>
                <FileTree.File name="Select.styled.tsx" />
            </FileTree.Folder>
        </FileTree.Folder>
    </FileTree.Folder>
</FileTree>
